package compile.lexical.token;

public enum TokenType {
    ASSIGN, BREAK, COMMA, CONST, CONTINUE, DIV, ELSE, EQ, FLOAT, FLOAT_LIT, GE, GT, ID, IF, INT, INT_LIT, LB, LC, LE,
    LT, L_AND, L_NOT, L_OR, LP, MINUS, MOD, MUL, NE, PLUS, RP, RB, RC, RETURN, SEMICOLON, VOID, WHILE
}
